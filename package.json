{
  "name": "codex-hub-outreach-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && esbuild server.ts --platform=node --format=esm --bundle --external:vite --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "vite": "^7.0.0",
    "express": "^5.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3",
    "tsx": "^4.20.3"
  },
  "engines": {
    "node": ">=20"
  }
}
